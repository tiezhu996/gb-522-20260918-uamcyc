# gb-522 光纤 OTDR 故障定位

请生成英文短名为 `fiber-otdr-fault-localization` 的 Go 全栈 Web 项目。系统面向光纤线路维护分析人员，导入离线 OTDR 采样点，识别事件、与基线轨迹比较并形成故障定位复核结论。它不连接真实 OTDR 或网管系统，不自动派工、切换链路或控制设备，定位结果只作分析参考且必须人工复核。项目不包含工单、库存、采购、订单、计费或通用文件管理功能。

## 项目主要需求

### 1. 核心实体

以下 4 个实体必须贯穿 PostgreSQL、Go 各后端层以及前端 types/api/store/page：

- `FiberRoute`：字段至少包含 `id`、`route_code`、`name`、`length_m`、`refractive_index`、`launch_connector`、`route_status`、`baseline_trace_id`、`created_at`；折射率范围和线路长度必须校验。
- `TraceCapture`：字段至少包含 `id`、`route_id`、`wavelength_nm`、`pulse_width_ns`、`sample_interval_ns`、`raw_points_json`、`noise_floor_db`、`captured_at`、`uploaded_by`；保留原始点和处理参数以便重放。
- `EventMarker`：字段至少包含 `id`、`trace_id`、`distance_m`、`event_type`、`insertion_loss_db`、`reflectance_db`、`confidence`、`reviewed`、`review_note`；必须关联轨迹并支持人工修订但保留算法原值。
- `LocalizationCase`：字段至少包含 `id`、`route_id`、`baseline_trace_id`、`current_trace_id`、`case_status`、`estimated_distance_m`、`uncertainty_m`、`conclusion`、`reviewer_id`、`closed_at`；状态迁移和结论变更写审计。

### 2. 核心页面

- `/routes` 线路档案：消费 `FiberRoute`、`TraceCapture`，设置基线并查看历史轨迹。
- `/traces` 轨迹分析：消费 `TraceCapture`、`EventMarker`，显示可缩放曲线、阈值和事件证据。
- `/events` 事件复核：消费 `EventMarker`、`FiberRoute`，支持筛选、修订类型和填写复核备注。
- `/cases` 定位案例：消费 `LocalizationCase`、`TraceCapture`，执行基线对比、形成结论和状态流转。
- `/audit` 审计页：消费 `LocalizationCase`、`EventMarker`，按线路、操作者和时间检索事件。

### 3. 差异算法与状态流

- 用纯 Go 实现确定性的轨迹处理管线：移动中值或滑动平均去噪、噪声底估计、一阶差分峰值检测、连续峰合并；所有阈值来自案例参数并记录。
- 根据采样时间、真空光速和线路折射率把样本位置换算为距离；实现边界检查，不能把超出线路长度的事件标记为有效。
- 基线比对使用距离容差匹配事件，输出新增、消失、损耗增大三类差异及置信度；算法结果与人工复核值分别保存。
- `LocalizationCase` 状态固定为 `draft -> analyzing -> pending_review -> confirmed -> closed`，分析失败回到 `draft` 并保存错误；只有 `reviewer` 能确认，关闭后不可修改。

### 4. 横切关注点

- JWT + RBAC 角色：`analyst`、`reviewer`、`admin`，贯穿数据库、认证/授权中间件、路由、前端守卫和按钮显隐。
- 不可变审计记录轨迹导入、基线变更、算法参数、事件修订、案例确认和关闭，包含 request ID 及前后值摘要。
- 统一错误处理区分数据格式错误、算法输入不足、状态冲突、无权限；前端全局错误提示保留可追踪错误码。
- 本地内存限流保护登录、轨迹导入和分析接口；结构化访问日志不得记录 JWT、原始密码或完整采样数组。

### 5. 共享枚举、组件、hooks 与中间件

- `EventType = connector | splice | bend | break | end | unknown`：后端 `backend/internal/constants/event.go`，前端 `frontend/src/types/event.ts`。
- `CaseStatus = draft | analyzing | pending_review | confirmed | closed`：后端 `backend/internal/constants/case.go`，前端 `frontend/src/types/case.ts`。
- README 列出枚举在数据库约束、model/dto/service/handler 与前端 store/component/page 的全部出现位置。
- 共享组件至少有 `TraceChart`（线路和轨迹页）、`EventTypeBadge`（轨迹和事件页）、`ReviewDialog`（事件和案例页）。
- hooks/composables 至少有 `useAuth`、`useTraceViewport`，集中处理权限和曲线视窗状态。
- 后端至少实现独立的 `RequestIDMiddleware`、`AccessLogMiddleware`、`AuthMiddleware`、`RBACMiddleware`、`RecoveryMiddleware`、`RateLimitMiddleware`。

### 6. 技术与规模红线

- 前端 Vue 3 + TypeScript + Vite + Element Plus + Pinia + ECharts，轨迹图必须渲染真实 API 数据。
- 后端 Go 1.22 + Gin + GORM + PostgreSQL；使用 validator/v10、结构化日志、构造器注入、事务、`%w` 和优雅停机。
- 功能 Go 代码 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行，不计测试、vendor、生成代码和前端。禁止单文件大杂烩和空壳凑数。
- API 统一 `/api/v1`，提供 `/healthz`、`/readyz`、统一响应/分页/错误码；算法包必须有表驱动测试和固定输入输出夹具。
- 禁止 TODO、placeholder、空函数、伪造算法结果或 handler 直接访问数据库。

### 7. 文件结构强制清单

根目录 `go.work` 必须 `use ./backend`，结构至少包含：

```text
./
├── go.work
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,algorithm,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

实体各自拆分 model、dto、repository、service、handler、router 文件；算法处理器按去噪、事件检测、距离换算、基线比对分文件，严禁合并职责。

`runtime_smoke.json` 只能作为启动 manifest，禁止保存构建、接口或 Browser 结果。必须提交以下可解析内容，并让后端在 smoke 环境选择 GORM SQLite、在 Compose 环境默认选择 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20522",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb522-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20522/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 项目名 `fiber-otdr-fault-localization`；前端 `18522`、后端 `19522`、PostgreSQL 宿主端口 `57522`，容器端口为 `80/8080/5432`。
- 完整提供 Compose、`.env`、`.env.example`、两个 Dockerfile、Nginx。Compose 顶层 `name`，不得含 `version:`；容器名带 `${COMPOSE_PROJECT_NAME:-fiber-otdr-fault-localization}` 前缀。
- PostgreSQL 使用命名卷与 healthcheck，后端等待数据库健康；后端和前端均配置 healthcheck，前端按后端健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。Nginx 将 `/api` 转发到 `backend:8080` 并配置 SPA fallback。
- 环境文件至少包含 Compose 名、三组端口、数据库变量、JWT 密钥、CORS 白名单、上传点数上限和日志级别；中文目录可直接 Compose 构建启动。
- README 用中文且 Docker 快速启动优先，包含 `cp .env.example .env`、访问地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、算法公式与限制、安全边界、开发命令、排错和 License。

### 9. 强制真实验收

1. 根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build`。
2. 执行 `docker compose config --quiet`、`docker compose up -d --build` 和 `docker compose ps`，确认数据库/后端 healthy、前端可访问，并验证 `/healthz`、`/readyz`。
3. 至少 8 项接口冒烟，覆盖登录、线路创建、轨迹导入、设基线、事件检测、事件复核、案例分析、案例确认、状态冲突和审计查询；方法、路径、状态码、断言和时间只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只使用 Codex 内置 Browser 验证登录、5 个页面、轨迹缩放、事件复核和案例确认，禁止外部 Chrome；截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 完成后执行 `docker compose down -v --remove-orphans` 并以 `docker compose ps` 确认无本项目容器，不得影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
